## What this changes

<!-- One or two sentences. -->

## Why

<!-- What was wrong, or what became possible. -->

## Checks

- [ ] `./gradlew :core:test` passes
- [ ] Built and run on a device, or explicitly noted as untested
- [ ] If `core/` changed: the security claims in `SECURITY.md` and the README still hold, or were updated with it
- [ ] If a DAT API call changed: verified against the version pinned in `gradle/libs.versions.toml`, not from memory
