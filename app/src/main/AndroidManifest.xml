<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- User role: broadcast the rotating proof. Admin role: scan for it.
         neverForLocation is accurate here and keeps the scan off the location permission. -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <!-- Required by the DAT SDK to reach the glasses through the Meta AI app. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- User role only: scanning the admin's enrollment QR. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- DAT registration hands off to the Meta AI app, which needs the network. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Keeps the beacon advertising with the screen off, which is what a real gate needs. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".GlassesGateApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.GlassesGate">

        <!-- Meta Wearables Device Access Toolkit registration. Developer Mode works with 0/0;
             a release build needs the real pair from the Wearables Developer Center. -->
        <meta-data
            android:name="com.meta.wearable.mwdat.APPLICATION_ID"
            android:value="${mwdatApplicationId}" />
        <meta-data
            android:name="com.meta.wearable.mwdat.CLIENT_TOKEN"
            android:value="${mwdatClientToken}" />

        <!-- GlassesGate has no use for glasses telemetry, so opt out of both SDK
             data collection channels rather than leaving the defaults on. -->
        <meta-data
            android:name="com.meta.wearable.mwdat.ANALYTICS_OPT_OUT"
            android:value="true" />
        <meta-data
            android:name="com.meta.wearable.mwdat.CRASH_REPORTING_OPT_OUT"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- How the Meta AI app hands control back after registration. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="glassesgate" />
            </intent-filter>
        </activity>

        <service
            android:name=".user.BeaconService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />
    </application>
</manifest>
